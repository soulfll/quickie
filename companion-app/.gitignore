.venv/
__pycache__/
*.pyc
config.json

# PyInstaller / Inno Setup build output (see BUILD_WINDOWS.md) -- these are
# generated from source on the Windows machine that builds them, not
# something to commit.
build/
dist/
installer/Output/
